# How to build?

## building main kernel

run `make defaultconfig && make menuconfig` to configure the kernel and run `make kernel` to build the kernel.

## building bootloader

run `make bootloader`.

## building bootable image

run `make bootable`.

# Info:
I'm working on the fs (rwfs) so most stuff won't work til' I get it UP n' RUNNING